[package]
name = "netsim-testing"
version = "0.1.0"
edition = "2021"

[dependencies]
bytes = "1"
futures = "0.3"
netsim-common = { path = "../common" }
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"

[features]
testing = []
