[package]
name = "link-api"
version = "0.1.0"
edition = "2021"

[dependencies]
actor-framework = { path = "../actor-framework" }
async-trait = "0.1"
mockall = { version = "0.13", optional = true }
netsim-model = { path = "../model" }
thiserror = "1.0"

[features]
testing = ["mockall"]
