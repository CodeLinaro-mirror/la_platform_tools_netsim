[package]
name = "link-api"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
mockall = { version = "0.13", optional = true }
netsim-model = { path = "../model" }

[features]
testing = ["mockall"]
